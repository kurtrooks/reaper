# To build ROS modules
> catkin_make install

# Dependiencies for eyes
> sudo pip3 install rpi_ws281x adafruit-circuitpython-neopixel
> eyes.py must be in the path

# To record audio
> rec filename.wav

# To playback files w/ modulation
> play filename.wav pitch -600 reverb 

# Apply voice change to input file, produce output file
> sox input.wav output.wav pitch -600 reverb

# To run reaper
> source install/setup.bash
> roslaunch joy_convert reaper.launch
